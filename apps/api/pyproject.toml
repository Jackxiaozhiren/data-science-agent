[project]
name = "dsa-api"
version = "0.1.0"
description = "Data Science Agent API"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dsa_api"]
