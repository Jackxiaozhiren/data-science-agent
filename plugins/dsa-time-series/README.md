# dsa-time-series — Flagship Plugin (V4 §81)

Separate package (§81): `forecasting + backtesting + forecast visualization`.

- Forecasting: wraps `packages/ml` forecast tool
- Backtesting: rolling window MAE
- Viz: forecast chart PNG

Own manifest (`manifest.yaml`), version `1.0.0`, permissions `read,compute`.
