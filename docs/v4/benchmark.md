# Benchmark Leaderboard & Dataset Hub — V4 W8 (§48–52)

Submission (§51) requires `system_name/version/commit/benchmark_version/model/configuration/seed/timestamp/results`.

Leaderboard (§50): `benchmarks/leaderboard/leaderboard.json` — ranked by `task_success_rate`.

Dataset Hub (§52): `benchmarks/v2/datasets/` + `examples/`, each with `license/source/citation/hash/version` (§52).
