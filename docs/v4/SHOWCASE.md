# V4 Showcases — (§80 8 Showcases)

1. **NL → Analysis**: `dsa analyze sales.csv --task "Analyze revenue decline"`
2. **Statistical Analysis**: correlation + hypothesis + regression (evaluator_v2 S01–S10)
3. **ML**: `train_model` + `forecast` via SDK `Agent.analyze`
4. **Evidence Trace**: `research/evidence-trace-showcase.md` — Claim→Evidence→Tool→Dataset
5. **Reproducibility**: `research/reproduction-showcase.md` — Run A→Fresh→Run B L0–L5 1.0
6. **Jupyter**: `apps/jupyter/README.md` — `await Agent().analyze()` inline
7. **MCP**: `dsa mcp` (Tools) + `POST /mcp-app/app` (App shell)
8. **Plugin**: `plugins/dsa-time-series` — `dsa plugin list` discovers `dsa-time-series v1.0.0`

Flagship integration (§82): Jupyter (W6) — then VS Code, MCP App.
