# MCP — V4 W4 (§29–32)

Levels (§30): Tools (L1) ✅, Resources (L2) ✅, Apps (L3) shell, Tasks (L4) future.

App (§31): `Dataset → Question → Analysis → Evidence → Viz` via `packages/mcp/src/dsa_mcp/app.py` mounted at `/mcp-app`.

Adapter (§32): `Core → Adapter → App`.
