# Jupyter / VS Code — V4 W6 (§38–42)

Jupyter: `from data_science_agent import Agent` renders `Analysis` (evidence + artifacts) inline; `%dsa` magic (§40) stub.

VS Code: light extension (§42) `Dataset Explorer / Ask DSA / Run Analysis / View Evidence`.

See `apps/jupyter/README.md` + `apps/vscode/README.md`.
