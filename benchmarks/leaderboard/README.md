# Benchmark Leaderboard & Dataset Hub — V4 W8 (§48–52)

Validated submissions only (§51):

```json
{
  "system_name": "DSA",
  "version": "3.0.0",
  "commit": "9ea647f",
  "benchmark_version": "0.3.0",
  "model": "stub/small",
  "configuration": {},
  "seed": 42,
  "timestamp": "2026-08-17T00:00:00Z",
  "results": { "task_success_rate": 1.0 }
}
```

Leaderboard: `leaderboard.json` (ranked by `task_success_rate`).

Dataset Hub: `benchmarks/v2/datasets/` + `examples/datasets/` — each with `license/source/citation/hash/version` (§52).
